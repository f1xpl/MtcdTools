<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.f1x.mtcdtools">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".service.MtcdService" />

        <receiver android:name=".BootCompletedBroadcastReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <activity android:name=".activities.actions.CreateActionActivity" />
        <activity android:name=".activities.CreateBindingActivity" />
        <activity android:name=".activities.ActionsListActivity" />
        <activity android:name=".activities.actions.KeyActionActivity" />
        <activity android:name=".activities.actions.LaunchActionActivity" />
        <activity android:name=".activities.actions.StartActivityActionActivity" />
        <activity android:name=".activities.actions.BroadcastIntentActionActivity" />
        <activity android:name=".activities.ObtainKeysSequenceActivity" />
        <activity android:name=".activities.actions.ManageActionsActivity" />
        <activity android:name=".activities.SelectActionActivity" android:theme="@android:style/Theme.Translucent.NoTitleBar" android:screenOrientation="landscape" />
    </application>

</manifest>
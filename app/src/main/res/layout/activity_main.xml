<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".activities.MainActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/AddNewBindingButton"
        android:id="@+id/buttonAddNewBinding"
        android:layout_alignParentEnd="true"
        android:layout_alignParentStart="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/RemoveBindingsButton"
        android:id="@+id/buttonRemoveBindings"
        android:layout_below="@+id/buttonAddNewBinding"
        android:layout_alignParentStart="true"
        android:layout_alignEnd="@+id/buttonAddNewBinding" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/EditModePackagesButton"
        android:id="@+id/buttonEditModePackages"
        android:layout_below="@+id/buttonRemoveBindings"
        android:layout_alignParentStart="true"
        android:layout_alignEnd="@+id/buttonRemoveBindings" />

</RelativeLayout>
